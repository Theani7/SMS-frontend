// Mock API by default - swap to real when backend is ready
export { mockLogin as login, mockRegister as register } from './mock';
// export { realLogin as login, realRegister as register } from './real';
